homepage:
    pattern:  /
    defaults: 
        _controller: MainBundle:Default:index
product_list:
    pattern: /products
    defaults:
        _controller: MainBundle:Default:list
product_show:
    pattern: /products/{product_slug}
    defaults:
        _controller: MainBundle:Default:show
    requirements: { _method: get, 'product_slug': '[\w\d\-]{1,32}'}
